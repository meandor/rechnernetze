title Logout

User->NetworkManager: logout()
activate NetworkManager

loop
    NetworkManager -> Active Peer : propagateLogout()
    activate Active Peer
    
    Active Peer -> Active Peer : removeNode()
    deactivate Active Peer 
end